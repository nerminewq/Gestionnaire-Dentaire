<form method="post" action="{{ path('traitement_delete', {'id': traitement.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce traitement ?');" style="display: inline-block;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ traitement.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

